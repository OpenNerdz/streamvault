{
  "name": "streamvault",
  "version": "0.1.0",
  "description": "A lean, local-first video downloader with a clean web UI.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "npm test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ffmpeg-static": "^5.3.0",
    "yt-dlp-exec": "^1.0.2"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
